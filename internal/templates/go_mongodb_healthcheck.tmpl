package healthcheck

import "github.com/gofiber/fiber/v2"

type Controller interface {
	Get(*fiber.Ctx) error
}

type controller struct{}

func (controller) Get(c *fiber.Ctx) error {
	return c.JSON(map[string]bool{
		"status": true,
	})
}

func NewController() Controller {
	return &controller{}
}
